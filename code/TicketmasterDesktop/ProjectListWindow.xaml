<Window x:Class="TicketmasterDesktop.ProjectListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Your Projects" Height="350" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Projects You're Involved With" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"
                   Grid.Row="0"/>

        <ListView x:Name="ProjectsListView" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Project Name" Width="200" DisplayMemberBinding="{Binding ProjectName}" />
                    <GridViewColumn Header="Description" Width="350" DisplayMemberBinding="{Binding Description}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>